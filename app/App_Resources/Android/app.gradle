android {
  defaultConfig {
    minSdkVersion 17
    targetSdkVersion 28
    compileSdkVersion 28
    multiDexEnabled true
  }
}

allprojects {
    repositories {
        google()
        jcenter()
        maven {
            url 'https://maven.google.com'
        }
    }
}